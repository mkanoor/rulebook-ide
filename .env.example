# Ansible Rulebook Editor - Environment Variables Example
# Copy this file to .env.local for local development (not tracked by git)
# ⚠️  NEVER commit .env files to git - they contain sensitive data!

# Server Port (default: 5555)
# This port is used for both the Express server and WebSocket
PORT=5555

# Ansible Rulebook Path (override default)
# ANSIBLE_RULEBOOK_PATH=/path/to/your/venv/bin/ansible-rulebook

# Ngrok API Token (required for Cloud Tunnels)
# Get your token from: https://dashboard.ngrok.com/get-started/your-authtoken
# ⚠️  SENSITIVE - Never commit this value!
# NGROK_API_TOKEN=your_ngrok_token_here

# Node Environment (development|production)
# NODE_ENV=development
